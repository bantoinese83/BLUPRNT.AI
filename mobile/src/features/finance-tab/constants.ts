import { TAB_BAR_HEIGHT, TAB_BAR_MARGIN } from "@/constants/Layout";

/** Bottom inset used for scroll padding and FAB positioning on the Finance tab. */
export const FINANCE_TAB_BAR_OFFSET = TAB_BAR_HEIGHT + TAB_BAR_MARGIN + 20;
