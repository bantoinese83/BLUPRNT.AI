import { TAB_BAR_SCROLL_PADDING } from "@/constants/Layout";

/** Same bottom clearance as tab scroll views (`ScreenWrapper`); keeps lists and FABs clear of the dock. */
export const FINANCE_TAB_BAR_OFFSET = TAB_BAR_SCROLL_PADDING;
