/** Presets aligned with common mobile layouts (dp; SVG viewBox scales uniformly). */
export const SnurraSize = {
  inline: 22,
  compact: 28,
  sheet: 40,
  screen: 120,
  /** Onboarding / hero loading — ~web `Loader` xl visual weight */
  hero: 200,
} as const;
