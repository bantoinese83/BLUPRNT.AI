/**
 * Shared layout constants to avoid circular dependencies between components and app layouts.
 */

export const TAB_BAR_HEIGHT = 90;
export const TAB_BAR_MARGIN = 0;

/** Scroll views & fixed docks: clearance above notched tab bar + center FAB. */
export const TAB_BAR_SCROLL_PADDING = TAB_BAR_HEIGHT + TAB_BAR_MARGIN + 8;
