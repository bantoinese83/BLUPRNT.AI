/**
 * List prices shown in product UI. Keep in sync with Stripe Price amounts
 * (web checkout) and App Store / RevenueCat products (mobile).
 */
export const PRICING = {
  architectUsdPerMonth: 12,
  projectPassUsdOneTime: 9,
} as const;
