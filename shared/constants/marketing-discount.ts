/** Stripe promotion code customers enter at checkout (must exist in Stripe Dashboard). */
export const MARKETING_DISCOUNT_PROMO_CODE = "BLUEPRINT35";

export const MARKETING_DISCOUNT_PERCENT = 35;

/** Set when user completes exit-intent / lead capture — unlocks auto-apply at checkout. */
export const MARKETING_DISCOUNT_SESSION_KEY = "marketing_discount_unlocked";
